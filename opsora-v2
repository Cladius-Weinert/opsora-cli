#!/usr/bin/env bash
# Opsora v2 Launcher — Codex/Cursor Edition
set -euo pipefail

# Source environment
if [ -f "$HOME/.opsora_env" ]; then
    source "$HOME/.opsora_env"
fi

# Use system Python (has all deps)
exec python3 "$HOME/opsora-cli/opsora_cmd/opsora_v2.py" "$@"
